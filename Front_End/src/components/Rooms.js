// // Component for Rooms
// const Rooms = ({ rooms, roomTypes, roomStyles }) => {
//     return (
//       <div>
//         <h2>Rooms</h2>
//         <ul>
//           {rooms.map((room) => (
//             <li key={room.id}>
//               Type: {roomTypes.find((type) => type.id === room.type_id).name},
//               Style: {roomStyles.find((style) => style.id === room.style_id).name},
//               Color: {roomColors.find((color) => color.id === room.color_id).name}
//             </li>
//           ))}
//         </ul>
//       </div>
//     );
//   };
  